const ERR_MESSAGE = {
  conf: {
    missed: "config missed",
    wrong: "wrong config",
    dupl: "duplicated config condition",
  },
  params: {
    dupl: 'duplicated parameters was found'
  }
};

module.exports = { ERR_MESSAGE };
